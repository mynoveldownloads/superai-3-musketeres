# 🛠️ Agent Task Prompt: Initialize New Module

**Instructions for User:** Copy this file into your target project directory, fill in the `<placeholders>`, and send the contents to the LLM agent as your first prompt.

---

**Prompt:**

Inside the `Template instructions` folder, read all `*_TEMPLATE_INSTRUCTIONS.md` and `*_MAKER_INSTRUCTIONS.md` files to understand the repository's strict formatting and structural rules.

Then, in the `<Target Directory Name>` folder, create `<main-file-name>.md` to do the following:
- <Describe the primary goal, e.g., brief the user through on how to instantiate an initial push>
- <List specific requirements, e.g., provide command steps to make commit and push>
- <List critical constraints, e.g., ensure the default branch is main>

Afterwards, read `UPDATE_CHANGES_TEMPLATE_INSTRUCTIONS.md` to formulate `UPDATE_CHANGES.md` for this directory.

Then, read `LOOK_HERE_MAKER_INSTRUCTIONS.md` to create `LOOK_HERE.md` onboarding instructions for this directory.

After everything is done, make sure to update all changes made into `UPDATE_CHANGES.md`.
